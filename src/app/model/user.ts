export type User = {
  userId?: number;
  username: string;
  firstName: string;
  lastName: string;
};

export type UserDto = {
  user_id?: number;
  username: string;
  first_name: string;
  last_name: string;
};
