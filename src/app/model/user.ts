export type User = {
	userId?: string;
	username: string;
	firstName: string;
	lastName: string;
};

export type UserDto = {
	user_id?: string;
	username: string;
	first_name: string;
	last_name: string;
};
