export type User = {
	username: string;
	firstName: string;
	lastName: string;
};

export type UserDto = {
	username: string;
	first_name: string;
	last_name: string;
};

export type UserFormDto = {
	first_name: string;
	last_name: string;
};

export type UserForm = {
	firstName: string;
	lastName: string;
};
